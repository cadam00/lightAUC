withr::with_options(list(repos = c(CRAN = "https://cloud.r-project.org/")),
{callr::default_repos()
rcmdcheck::rcmdcheck(args = c("--no-manual", "--as-cran")) })
devtools::check_win_devel()
devtools::check_win_release()
devtools::check_mac_release()
devtools::build(manual=TRUE)
priorCON::get_outputs
Rcpp::sourceCpp("C:/Users/Administrator/Desktop/arma_roc.cpp")
n <- 1e7
probs <- runif(n)
actual <- as.integer(sample(c(1,0), n, replace=T))
bench::mark(fast_auc_code_old(probs, actual),
fast_auc_code_new(probs, actual))
Rcpp::sourceCpp("C:/Users/Administrator/Desktop/arma_roc.cpp")
bench::mark(fast_auc_code_old(probs, actual),
fast_auc_code_new(probs, actual))
Rcpp::sourceCpp("C:/Users/Administrator/Desktop/arma_roc.cpp")
bench::mark(fast_auc_code_old(probs, actual),
fast_auc_code_new(probs, actual))
bench::mark(fast_auc_code_new(probs, actual),
fast_auc_code_old(probs, actual))
Rcpp::sourceCpp("C:/Users/Administrator/Desktop/arma_roc.cpp")
bench::mark(fast_auc_code_old(probs, actual),
fast_auc_code_new(probs, actual))
bench::mark(fast_auc_code_old(probs, actual),
fast_auc_code_new(probs, actual))
Rcpp::sourceCpp("C:/Users/Administrator/Desktop/arma_roc.cpp")
bench::mark(fast_auc_code_old(probs, actual),
fast_auc_code_new(probs, actual))
bench::mark(fast_auc_code_old(probs, actual),
fast_auc_code_new(probs, actual))
bench::mark(fast_auc_code_old(probs, actual),
fast_auc_code_new(probs, actual))
bench::mark(fast_auc_code_new(probs, actual),
fast_auc_code_old(probs, actual))
bench::mark(fast_auc_code_old(probs, actual),
fast_auc_code_new(probs, actual), min_iterations = 10)
bench::mark(fast_auc_code_new(probs, actual),
fast_auc_code_old(probs, actual), min_iterations = 10)
bench::mark(fast_auc_code_new(probs, actual),
fast_auc_code_old(probs, actual), min_iterations = 30)
n <- 1e2
probs <- runif(n)
actual <- as.integer(sample(c(1,0), n, replace=T))
bench::mark(fast_auc_code_new(probs, actual),
fast_auc_code_old(probs, actual), min_iterations = 30)
bench::mark(fast_auc_code_new(probs, actual),
fast_auc_code_old(probs, actual), min_iterations = 30)
bench::mark(fast_auc_code_new(probs, actual),
fast_auc_code_old(probs, actual), min_iterations = 30)
bench::mark(fast_auc_code_new(probs, actual),
fast_auc_code_old(probs, actual), min_iterations = 30)
bench::mark(fast_auc_code_new(probs, actual),
fast_auc_code_old(probs, actual), min_iterations = 30)
bench::mark(fast_auc_code_old(probs, actual),
fast_auc_code_new(probs, actual), min_iterations = 30)
bench::mark(fast_auc_code_old(probs, actual),
fast_auc_code_new(probs, actual), min_iterations = 30)
bench::mark(fast_auc_code_old(probs, actual),
fast_auc_code_new(probs, actual), min_iterations = 30)
bench::mark(fast_auc_code_old(probs, actual),
fast_auc_code_new(probs, actual), min_iterations = 30)
bench::mark(fast_auc_code_old(probs, actual),
fast_auc_code_new(probs, actual), min_iterations = 30)
bench::mark(fast_auc_code_old(probs, actual),
fast_auc_code_new(probs, actual), min_iterations = 30)
bench::mark(fast_auc_code_old(probs, actual),
fast_auc_code_new(probs, actual), min_iterations = 30)
bench::mark(fast_auc_code_old(probs, actual),
fast_auc_code_new(probs, actual), min_iterations = 30)
n <- 1e3
probs <- runif(n)
actual <- as.integer(sample(c(1,0), n, replace=T))
bench::mark(fast_auc_code_old(probs, actual),
fast_auc_code_new(probs, actual), min_iterations = 30)
bench::mark(fast_auc_code_old(probs, actual),
fast_auc_code_new(probs, actual), min_iterations = 30)
bench::mark(fast_auc_code_old(probs, actual),
fast_auc_code_new(probs, actual), min_iterations = 30)
bench::mark(fast_auc_code_old(probs, actual),
fast_auc_code_new(probs, actual), min_iterations = 30)
bench::mark(fast_auc_code_old(probs, actual),
fast_auc_code_new(probs, actual), min_iterations = 30)
n <- 1e7
probs <- runif(n)
actual <- as.integer(sample(c(1,0), n, replace=T))
bench::mark(fast_auc_code_old(probs, actual),
fast_auc_code_new(probs, actual), min_iterations = 30)
